import { Text, View } from 'react-native';

export default function ShipPlacement() {
  return (
    <View className={'juistify-center w-full flex-1 items-center'}>
      <Text className="text-3xl text-white">Ship placement</Text>
    </View>
  );
}
